import React from 'react';

const PageTitleServicesInside = (props
) => {
    return (
        <div id="page-title">
            <div className="wrapper text-center">
                <h1 className="large text-uppercase">{props.name}</h1>
            </div>
        </div>
    );
};

export default PageTitleServicesInside;
